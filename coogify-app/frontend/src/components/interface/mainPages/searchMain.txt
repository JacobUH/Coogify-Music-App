const uniqueAlbumNames = new Set<string>();

  const filteredAlbums = albums.filter((album) => {
    const albumName = album.albumName.toLowerCase();
    const artistName = album.artistName.toLowerCase();
    const includesSearchInput =
      albumName.includes(searchInput.toLowerCase()) ||
      artistName.includes(searchInput.toLowerCase());

    // Check if the album name is unique
    if (!uniqueAlbumNames.has(albumName) && includesSearchInput) {
      uniqueAlbumNames.add(albumName);
      return true;
    }

    return false;
  });